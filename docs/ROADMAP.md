# Roadmap

0.1.x:

- Keep safe reject as the default policy.
- Expand AST-level planner checks for alias imports, local classes, and side
  effect ordering.
- Improve report schema validation and JSON audit trails.
- Add more differential fixtures for package-scale closed-world scans.

Future:

- Optional Linux mutation job.
- More precise import graph analysis.
- Richer verification case generation while documenting that observation is not
  proof.
